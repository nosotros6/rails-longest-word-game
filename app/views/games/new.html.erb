<h1>New Game<h1>

<%= @letters %>

<p class="form-prefix">What is the longest word can you find?</p>

<form action="/score" method="POST" class="form-inlie">
  <%= hidden_field_tag :authrnticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>
  <div class="input-group input-group">
    <input type="text" name="word" class="form-control">
    <span class="input-group-btn">
      <input type="submit" value="Enviar" class="btn btn-default">
    </span>
  </div>
</form>
